{
  "private": true,
  "scripts": {
    "create": "pg-migrate create",
    "migrate": "pg-migrate up",
    "reverse": "pg-migrate down",
    "prestart": "npm run migrate",
    "start": "postgraphql $DATABASE_URL --schema ambethia"
  },
  "dependencies": {
    "node-pg-migrate": "0.0.10",
    "postgraphql": "^1.3.0"
  }
}
