{
  "private": true,
  "scripts": {
    "prestart": "exodus migrate",
    "start": "postgraphql $DATABASE_URL --schema ambethia --hostname 0.0.0.0"
  },
  "dependencies": {
    "exodusjs": "^2.0.4",
    "postgraphql": "^1.3.0"
  }
}
